info:
  name: Get Updates
  type: http
  seq: 3

http:
  method: POST
  url: https://api.telegram.org/bot{{BOT_TOKEN}}/getUpdates
  body:
    type: json
    data: ""
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
