info:
  name: Get Self
  type: http
  seq: 1

http:
  method: GET
  url: https://api.telegram.org/bot{{BOT_TOKEN}}/getMe
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
